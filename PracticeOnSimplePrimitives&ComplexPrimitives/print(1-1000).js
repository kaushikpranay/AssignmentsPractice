
const { clear } = require("console");

var counts = 10;

function counting(count){
        
        var ans = console.log(count);
        console.log(clear);
        return ans;
}

for(let i=0;i<counts;i++){
    counting(i);
}